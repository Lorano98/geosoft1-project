extends layout

block content
  h1= title
  .row.justify-content-md-center
    .col-lg-4.col-xs-12.mb-3
      .card.shadow.p-4
        form(action='/edit/finish' method='post')
            fieldset
                .form-group.row
                    label.col-form-label(for='id') id
                    div
                      input#id.form-control(type='text' name="id" placeholder='id' readonly='')
                .form-group.row
                    label.col-form-label(for='name') Name
                    div
                      input#name.form-control(type='text' name="name" placeholder='Name')
                .form-group.row
                    label.col-form-label(for='hoehe') Höhe
                    div
                      input#hoehe.form-control(type='number' name="hoehe" placeholder='Höhe in m')
                .form-group.row
                    label.col-form-label(for='url') Url
                    div
                      input#url.form-control(type='text' name="url" placeholder='Url')
                .form-group.row
                    label.col-form-label(for='x') X
                    div
                      input#x.form-control(type='text' name="x" placeholder='X Koordinate')
                .form-group.row
                    label.col-form-label(for='y') Y
                    div
                      input#y.form-control(type='text' name="y" placeholder='Y Koordinate')
                button.btn.btn-primary.mb-2(type='submit' value="speichern" disabled='')#bearbeitenSpeichern Speichern
                button.btn.btn-primary.mb-2(type='submit' formaction="/edit/del_notification" disabled='')#bearbeitenLoeschen Löschen
    .col-lg-8.col-xs-12.mb-3
      .card.shadow.p-4(style= 'height: 100%;')
        h3 Gebirge anklicken:
        #map

  script.
    let geojson = !{JSON.stringify(data)}
    // Set active Button
    document.querySelectorAll(".active")[0].classList.remove("active");
    document.querySelectorAll(".nav-link")[2].classList.add("active");

  script(src='javascripts/gebirgeBearbeiten.js')
 
